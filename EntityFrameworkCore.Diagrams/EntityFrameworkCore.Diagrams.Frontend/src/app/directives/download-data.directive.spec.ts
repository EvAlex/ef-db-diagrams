import { DownloadDataDirective } from './download-data.directive';

describe('DownloadDataDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadDataDirective();
    expect(directive).toBeTruthy();
  });
});
