import { ScalableDirective } from './scalable.directive';

describe('ScalableDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalableDirective();
    expect(directive).toBeTruthy();
  });
});
