import { ScrollbarWidthDirective } from './scrollbar-width.directive';

describe('ScrollbarWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbarWidthDirective();
    expect(directive).toBeTruthy();
  });
});
