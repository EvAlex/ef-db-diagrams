import { DevicemotionScrollDirective } from './devicemotion-scroll.directive';

describe('DevicemotionScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DevicemotionScrollDirective();
    expect(directive).toBeTruthy();
  });
});
