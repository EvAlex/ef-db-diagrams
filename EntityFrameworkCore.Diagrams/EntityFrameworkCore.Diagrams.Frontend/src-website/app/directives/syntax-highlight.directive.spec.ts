import { SyntaxHighlightDirective } from './syntax-highlight.directive';

describe('SyntaxHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SyntaxHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
